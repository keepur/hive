# Hive instance definitions — one per line
# Fields: INSTANCE_ID | HIVE_CONFIG | (unused) | LAUNCHAGENT_LABEL | LOGS_DIR | PORTS (space-separated)
#
# HIVE_CONFIG is relative to DEPLOY_DIR
# Agents are stored in MongoDB (agent_definitions collection), not on disk
# PORTS are checked/killed on deploy restart

dodi|hive.yaml|-|com.hive.agent|logs|3100 3200
personal|hive-personal.yaml|-|com.hive.personal.agent|logs-personal|3400 3403
