# Hive instance definitions — one per line
# Fields: INSTANCE_ID | HIVE_CONFIG | AGENTS_PATH | LAUNCHAGENT_LABEL | LOGS_DIR | PORTS (space-separated)
#
# HIVE_CONFIG and AGENTS_PATH are relative to DEPLOY_DIR
# PORTS are checked/killed on deploy restart

dodi|hive.yaml|agents|com.hive.agent|logs|3100 3200
personal|hive-personal.yaml|agents-personal|com.hive.personal.agent|logs-personal|3400 3403
