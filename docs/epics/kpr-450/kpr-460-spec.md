# KPR-460 — Model catalog scanner lifecycle and truthful freshness

## TL;DR

Run Claude, Codex, and Grok subscription discovery automatically every eight hours per instance, with an immediate startup check for unseeded or overdue catalogs. Reuse KPR-459's shared store and fenced attempt tokens so provider failures, restarts, overlapping ticks, and uncertain writes cannot erase a working catalog or claim a successful check that never committed. Expose the last catalog write, last successful discovery, latest attempt, and pending export separately through the existing catalog tool and an informational doctor section; KPR-461 owns CoS delivery.

## Key Points

- Use one engine-owned `ModelCatalogScanner` with a one-minute maintenance tick and independent provider lanes. Each provider starts at most one normal discovery every eight hours, anchored to its persisted attempt start; pass the exact due-read observation into the atomic claim so a delayed scanner cannot reuse eligibility after another attempt completes.
- Startup checks absent/empty catalogs and providers with no discovery history immediately, checks overdue providers, and respects active leases. A completed recent attempt on a seeded catalog is not repeated solely because the engine restarted.
- ⚠ An expired running attempt is unresolved interrupted work, not a recorded failure. A delayed acknowledged claim can return `started` after expiry without authorizing discovery; recheck invocation/cancellation/lease, then recover and use a fresh eligible observation for takeover. Mongo's operation timestamp does not advance during a server pause.
- Keep the **same store instance** from fresh attempt acquisition through first `applyDiscovery`. KPR-459's bounded store-local proof controls submission; restarted stores and uncertain-token reentry reconcile only and wait for positive evidence or safe lease supersession.
- Successful unchanged discovery updates successful-check time without changing snapshot write time or creating history/change records. Manual refresh never changes discovery cadence or successful freshness; a later manual write is explicitly distinct from the most recent discovery check.
- Isolate discovery and storage failures by provider, retain the last committed models, and sanitize errors. Continue maintenance while agents are stopped; stop the scanner only on engine shutdown, abort external work, and drain for a bounded time before closing Mongo.
- ⚠ Start with fixed defaults: eight-hour discovery cadence, one-minute maintenance, two-minute attempt lease, and five-second shutdown drain. These are engineering constants with injected test clocks/timers, not new user configuration or hot-reload behavior.
- Gemini remains live-only and plugin catalogs remain manually maintained. Export recovery can cover all stored providers, but this child creates no notifications, dispatch callbacks, agent assignments, capability updates, or new catalog read tool.

## Authority and dependency baseline

[KPR-450 Gate 1 approval](https://linear.app/keepur/issue/KPR-450/automatic-provider-model-catalog-scanner-3xday#comment-9580cb2b) delegates the eight-hour/startup policy, failure isolation, truthful diagnostics, shared manual/scanner writer, and routine engineering choices. The operator subsequently approved `drive-epic` in place of the absent `/spec-and-implement` command. Waterfall maturation remains binding: this is a specification, not authorization to begin delivery ahead of the remaining child gates.

The epic description has no `## Decision Register — Canon` section and no merged-child canon exists. Proceed without one. KPR-459 is mature but **unimplemented**; [its specification](./kpr-459-spec.md) at `43298fd` and [its final reviewed plan](./kpr-459-plan.md) at `5b12a0f`, especially “Attempt fencing primitives for KPR-460,” “Acquisition contract,” “Uncertainty rules,” and “Handoff to Siblings,” define the planned contracts consumed here. These revisions add atomic observed-state acquisition and distinguish Mongo's operation timestamp from time at acknowledgment. Do not infer these modules already exist in product code or weaken their rules to simplify scheduling. If implementation changes those contracts, reconcile this specification before delivering KPR-460.

## Problem, goals, and exclusions

The current engine never starts deterministic subscription-model discovery. Stored catalogs can age silently, and a catalog's `updatedAt` describes a saved list rather than the last successful vendor check. A heartbeat needs to survive partial provider failures and restarts without delaying engine startup, duplicating a provider's work, or treating an uncertain Mongo acknowledgment as a failed discovery.

This child owns timing, engine startup/shutdown, bounded local concurrency, interpretation of the shared store's outcomes, background export recovery, and readable freshness/status diagnostics. It must leave enough durable information to distinguish a recent successful check from a recent failed attempt even when the engine is offline.

Excluded: KPR-459's provider protocol/auth/normalization or writer redesign; KPR-461's CoS selection, delivery/retry/acknowledgment and notification diagnostics; Gemini polling/storage; plugin discovery; user model assignments; generation probes; scheduled agent turns; provider circuit-breaker changes; `LLM_CATALOG`; effort suffixes; Beekeeper; global default-agent routing; cross-instance coordination; deployment; KPR-320 and KPR-315. No cron entry, new Mongo client in the engine, extra scanner-status collection, or general scheduling framework is required.

## Code grounding and integration points

- `src/index.ts` obtains one shared Mongo client, verifies the identity sentinel, and passes `guardDb(rawDb, writeGuard)` to engine consumers. Construct the scanner's long-lived `ModelCatalogStore` from that guarded `Db`; never use `rawDb` or a new connection for catalog state. The admin tool may retain its separate store object for manual writes because store-local submission proof is needed only within each scanner attempt.
- `src/agents/spawn-coordinator-heartbeat.ts`, `src/agents/circuit-breaker-heartbeat.ts`, and `src/memory/memory-lifecycle-heartbeat.ts` supply the structural pattern: engine-owned class, idempotent lifecycle, unreferenced interval, contained failures, and callable maintenance for tests. Their intervals allow overlapping asynchronous calls, so copy the ownership pattern while adding explicit lane guards.
- Add `src/admin/model-catalog-scanner.ts` for lifecycle and `src/admin/model-catalog-status.ts` for pure shared status derivation. Consume KPR-459's planned `model-catalog-types`, `model-catalog-discovery`, and `model-catalog-store`; keep provider implementation and SDK imports out of the pure status module and doctor path.
- `src/admin/admin-mcp-server.ts` already returns a JSON entries array followed by text notes. Preserve that structure, existing entry fields, provider ordering, plugin support, and Gemini behavior. Add built-in discovery status as provider notes and revise descriptions that still say subscription catalogs require manual research.
- `src/cli/doctor-checks.ts` already reads durable collections directly for some diagnostics; add a read-only catalog adapter there, and render an informational “Model catalogs” section in `src/cli/doctor.ts`. Reuse the pure status derivation so doctor and tool do not disagree about freshness.
- `src/boot-order.test.ts` guards engine wiring order. Place scanner startup in the existing heartbeat area after provider/plugin wiring and guarded database initialization; it does not need a new exception to the spawn-capable-boundary guard. Update lifecycle wiring verification without importing side-effecting `index.ts` to boot the live engine in tests.
- Update the catalog behavior documented in `CLAUDE.md` and `docs/providers.md` when implementation lands. No `hive.yaml`, `.env`, credentials, or deployment-clone edits are needed by this child.

## Lifecycle contract

Expose a small testable class with `start(): void`, `tick(): Promise<void>`, and `stop(): Promise<void>`. The constructor receives the shared store and discovery function; clock, UUID, timer, and timing overrides are injected for tests. Production imports one configured discovery function created by KPR-459. There is no dependency on `AgentManager`, the registry's assigned models, scheduler jobs, or `Dispatcher`.

`start` is idempotent. It starts an unreferenced one-minute interval and launches one immediate startup tick without awaiting provider network work. All started promises have rejection handlers. The engine must become available even if all provider credentials are absent or discovery is slow. Construction/start cannot perform a mandatory successful discovery or index write as a boot gate.

`tick` launches independently eligible provider lanes and the export-recovery task, returning when the work it launched settles. A lane is marked busy synchronously before its first await. A concurrent timer/test tick skips that lane, with no queued catch-up tick or replacement of its in-flight promise. A slow Claude lane cannot stop Codex/Grok from being considered by later ticks. The recovery task has its own no-overlap latch and cannot gate launching a provider lane. Use per-task containment/settled results rather than one unhandled rejection aborting a tick.

`start` called again while active is a no-op. `stop` is terminal for that object; subsequent `tick`/`start` calls do not restart work. A new engine lifecycle creates a new scanner. `agentManager.stopAll()`, registry reload, and ordinary agent disable/enable operations do not stop or recreate it. Each instance scans its own database independently, as approved; persisted leases also protect accidental duplicate scanner objects using that same database.

### Timing and eligibility

Use `SCAN_INTERVAL_MS = 8 * 60 * 60 * 1000`, `MAINTENANCE_INTERVAL_MS = 60_000`, and `ATTEMPT_LEASE_MS = 120_000`. KPR-459 already bounds discovery to 60 seconds; the lease leaves another minute for completion. It is a fencing deadline, not a guarantee Mongo finishes within a minute. No heartbeat extends a discovery lease.

At each provider lane's decision, call `readCatalogState(provider)` and evaluate eligibility from its `snapshot`, retaining the opaque provider-bound `observed` object returned by that same read. Do not cache the last-attempt time as the authoritative schedule and do not use catalog `updatedAt`, manual version timestamps, or `lastSucceededAt` to postpone discovery. For a completed attempt without an applicable unseeded-startup exception, `nextAttemptAt = scan.startedAt + eight hours`, regardless of success or failure. Check `now >= nextAttemptAt`, so an exact-boundary tick is due. A delayed or suspended engine does one due attempt, never one attempt for every missed interval; the new persisted start anchors the next interval. The maintenance interval introduces at most approximately one minute of normal scheduling delay, excluding event-loop/storage delay.

The store's observation captures the complete BSON scan value and field presence, including absent/malformed values, plus whether models are a nonempty array. Keep it opaque: do not synthesize it from normalized status, replace it during acquisition, serialize it into the attempt, or persist it across restart. The conditional claim compares this observation atomically with lease availability and proposed-lease validity. Thus another scanner's intervening running/completed attempt invalidates the old due decision even after its lease is released. A manual first seed invalidates an unseeded observation; a manual edit preserving seeded status or export recovery alone does not invalidate eligibility. Catalog revision, names, notes, order, provenance, `updatedAt`, and `pendingExport` are deliberately outside the observation. An additional pre-acquisition read without passing its corresponding observation cannot close this race.

Apply these rules in order. Any due decision for an unseeded snapshot consumes this process's startup opportunity before begin, including decisions reached through the no-history or expired-running rows; a delayed competing scanner cannot save that exception for an immediate retry after another scanner fails.

| Observed state | Action |
| --- | --- |
| A local operation for this provider is still outstanding | Skip the provider. A timeout or expired persisted lease alone does not release this local no-overlap guard while an underlying store call is still running. |
| An unresolved local acquisition/completion identity is being tracked and its lease has not expired | Read/reconcile that identity only; do not start new external discovery or allocate a replacement attempt. |
| Persisted running attempt with an active lease, including one from another process | Skip discovery and leave its status untouched. Never resume another store's submission. |
| Persisted running attempt whose lease expired | Run the bounded reconciliation/recovery behavior below; if a fresh read still shows expired running work, acquire a new UUID using that read's observation. This interrupted-work recovery is immediate rather than an eight-hour wait. A predecessor completing after that read invalidates the claim. |
| No scan history, including a nonempty legacy/manual catalog | Due immediately: no automatic check has established a schedule yet. |
| Absent/empty models at the first eligible startup check | Due immediately, even if a previous process recently recorded a failed attempt. Consume this startup opportunity once per provider when using it for the due decision, before calling begin; `busy` or uncertainty does not re-arm it. An active lease defers the opportunity before consumption. Subsequent completed attempts follow the normal eight-hour cadence even if the catalog remains unseeded. |
| Completed scan with a valid start time and no applicable startup exception | Due at the persisted start plus eight hours. Restart of a seeded catalog, stale successful freshness after a recent failed attempt, or a manual edit does not bypass this gate. |

If an attempt cannot be acquired or its state cannot be read, do not contact that provider. `busy` means an acknowledged no-claim result, including a changed observation or proposal expired at the claim's operation timestamp, as well as an active competing lease. It is not a new attempt or failure. Discard that due decision and reread/reevaluate all eligibility rules on a later maintenance tick; do not refresh `observed`, reuse the UUID, or retry begin within the lane to force progress. A read/acquisition exception instead retains a sanitized storage diagnostic and follows uncertainty handling below. Provider discovery is limited to the exact KPR-459 built-in union, in its stable existing order; do not enumerate plugin providers or Gemini into the discovery loop. Missing credentials are handled as honest discovery failures, not a synthetic successful skip, and no paid API credential probe is added.

Invalid/missing scan dates and timestamps in the future must not produce a “recent” success or an unbounded scheduling delay. Expose unavailable/clock-inconsistent timing and consider a fresh attempt due once the persisted lease permits it. Never force-clear or bypass a future/invalid lease; the store's server-side lease checks remain authoritative, and an invalid lease that cannot be acquired is visible storage/state trouble. Normal production timestamps are BSON `Date` values; do not invent a successful timestamp from a legacy catalog's `updatedAt`.

### Attempt execution and the store-local proof

For a new attempt allocate one UUID, capture its start and two-minute lease expiry, and call `beginDiscoveryAttempt(provider, { attemptId, startedAt, leaseExpiresAt, observed: dueState.observed })` on the long-lived scanner store, using the observation retained from the read that made this attempt due. Preserve the original dates throughout the invocation; neither retries nor delayed responses renew them. Keep this exact store object and attempt identity for the first `applyDiscovery`. The store, not the scanner, determines whether the acquisition established a never-submitted proof. `started` can also result from a reread after ambiguous acquisition; it does not give the caller permission to bypass the proof or reconstruct it elsewhere.

KPR-459 performs at most one harmless missing-document shell insert and one conditional claim per begin, with at most one evidence reread after a thrown mutation. It never retries with a newer observation. The claim requires the stored lease to be absent or valid and expired against both sampled local time and Mongo's operation timestamp `$$NOW`, and the proposed lease to be strictly later than that operation timestamp. A malformed lease never becomes absent/expired. `$$NOW` stays constant during the operation: a claim admitted before expiry may pause and install an expired running token if its observation still matches. A changed predecessor observation still refuses the mutation. Neither the lease filter nor a client delegation barrier establishes that the lease was unexpired at eventual wall-clock mutation time.

An acknowledged match returns `started` even if its response arrives after expiry or known ownership ends; `busy` would falsely claim no acquisition. The store creates proof only after resampling time and verifying its acquiring invocation is still current, and never replaces or clears a newer proof with an old response. Independently, after `begin` returns and immediately before external discovery, verify that this is still the current provider-lane invocation, the scanner is active, its signal is not canceled, and the original supplied lease is strictly later than freshly sampled local time. An expired/obsolete `started` starts no provider request; interrupted-work read/recovery occurs only through a current active lane, without a fabricated failure. A stopped or obsolete invocation starts no further scanner step. When those checks pass, call the injected discovery function with that provider's abort signal.

After discovery resolves, check the current invocation, cancellation/stopped state, and original lease again before calling `applyDiscovery`. The store rechecks the original proof lease before consumption and its persisted token/revision/operation-timestamp filters before submission; these checks do not strengthen `$$NOW` into an advancing wall clock. A raced/ambiguous `started` may yield rows followed by a reconciliation-only `applyDiscovery`; accept the resulting uncertainty and do not re-fetch to force progress. A thrown acquisition does not authorize external discovery, even if a later read finds the token.

No rows, hashes, or fresh-submission proof are persisted by the scanner. At most three provider lane records hold an attempt identity, controller, outstanding promise, and any rows needed for that active operation. Discard response rows after the initial apply returns; later reconciliation uses the same UUID and a non-replayed input with the existing store API. KPR-459's no-proof `applyDiscovery` branch reconciles before row validation, so a restarted store can call it with no replacement payload (for example `[]`) solely to look for positive evidence. This is not a new public replay API and must never enter the submission path. Do not call it as a reconciliation shortcut on a never-submitted local attempt whose proof is still ready.

Interpret outcomes as follows:

| Result | Required lifecycle behavior |
| --- | --- |
| Discovery throws a typed provider/auth/timeout/validation error before apply | Record that safe failure through `failDiscoveryAttempt` while active and while the token can still be completed. Preserve models and `lastSucceededAt`; the completed attempt schedules the next normal check eight hours from its start. |
| `committed`, with or without `recoveryPending` | The discovery succeeded. Read authoritative stored status as needed; do not call failure recording or undo the catalog. Pending projection is separate maintenance work. |
| `unchanged` | Successful check, no catalog content write/history/change. The shared store alone advances `lastSucceededAt`; cadence still uses the attempt start. |
| `not-committed` | This is definitive refusal supplied by the proof-aware store. Record a safe failure, including `storage` where applicable, if the token remains valid. Do not create an automatic immediate apply/re-discovery retry loop even if `retriable` is true; the normal next attempt is eight hours from this one's start. |
| `superseded` | Drop local response state and reread on a later tick. Do not record failure for the old token or mutate its successor. |
| `commit-unknown` | Retain the operation/attempt identity and uncertainty. Do not call `failDiscoveryAttempt`, replay rows, advance successful freshness, or allocate a new attempt until positive evidence or safe lease supersession permits progress. |
| Acquisition/failure-recording/storage call throws, or a started store call has not settled by its lease deadline | An error/deadline is not evidence that its mutation never happened. Retain the attempt identity, report storage/unfinished status, and follow reconciliation; do not turn a raw thrown error into a provider failure or free a still-outstanding local lane. |

Use KPR-459's `CatalogError`/safe-error helpers and fixed diagnostic text. No raw SDK/HTTP/Mongo/command exception, credential, URL bearer, or account metadata reaches logs or tool/doctor output. Logger name is `model-catalog-scanner`. Log attempt outcomes with provider, attempt ID, safe code/status, and recovery state; avoid a repeated warning every minute for the same unchanged error. A successful new check clears the prior provider error through the store's existing success update.

### Reconciliation and interrupted work

Read persisted state and recover pending exports on maintenance; after `applyDiscovery` returned `commit-unknown`, the proof-consumed same store or a fresh store can call `applyDiscovery` with that exact UUID for reconciliation only. An uncertain begin or failure-recording call that never entered apply instead uses read/recovery: its original store may still hold a ready proof, so do not pass placeholder rows to that store. Positive version/envelope evidence can establish the original changed commit. Positive `scan.outcome: succeeded` evidence establishes that a successful check was durably recorded even when the store's conservative unchanged-operation reconciliation lacks the captured old snapshot identity; do not label a later manual snapshot as that old check's content. The current document remains the authoritative view for freshness and future cadence.

An immediate negative read, unreachable history, failed index initialization, guard refusal now, or failed export recovery cannot establish noncommit of an earlier operation. Preserve uncertainty through all those paths. A restarted scanner does not call `begin` with an old UUID hoping to regain proof, reconstruct a fake successful result, or mark expired work failed. While an old lease remains active it waits. When the old lease has safely expired and a fresh read still shows expired running work, a fresh UUID acquisition may supersede it only with that same read's observation under KPR-459's rules. Once a successor changes the scan, delayed old acquisitions and completions cannot replace its running or completed state, including after its lease is released. A predecessor completing between the due read and takeover causes `busy`; reread on a later tick and apply the completed attempt's cadence. A late positive success observed before takeover returns to the normal schedule instead of triggering an unnecessary fresh request.

Accepted-but-expired `started` responses use this same interrupted-work path: retain the identity, discard any authority to start discovery, and use bounded read/export recovery before a later fresh eligible observation permits takeover. Do not call placeholder-row apply for a begin that never entered apply, renew the accepted dates, or write a synthetic failed/succeeded status. This permits a harmless expired running token when a server operation admitted before expiry or its acknowledgment was delayed, while preserving catalog content and successful freshness.

If an acquisition threw before its token was observable, retain that UUID and its supplied lease locally until it settles or expires. No new begin/provider request starts before then; an outstanding underlying store promise continues to hold the local lane even after expiry. After restart only durable evidence remains; acquire any new UUID using a fresh eligible observation and let the store's atomic observation/lease predicates decide whether it can proceed. A delayed old shell can create only an unseeded shell, and a delayed old claim cannot overwrite a successor's changed observation. Never regard a local timer as proof that a Mongo mutation was canceled.

This design deliberately permits safe stalled work: if an underlying store promise never settles, that provider remains locally occupied and other providers continue. Show unfinished/expired state; shutdown/restart can recover through persisted leases. Do not release the latch and create arbitrarily many uncancelable operations merely to make a timer appear healthy.

## Export maintenance and sibling boundary

Launch `recoverPendingExports()` once at startup and on the one-minute maintenance tick, guarded against an overlapping recovery sweep. This recovers pending exports for built-in and manually maintained plugin catalogs without discovering plugin/Gemini models. It can run independently of a provider's eight-hour schedule, successful discovery, or notification availability. KPR-459 catches export failures per provider; contain a sweep-level exception as storage trouble and retry on a later tick. Provider writes keep their own required recovery step, so a slow all-provider sweep never becomes a global prerequisite for discovery or correctness. Competing export recovery is idempotent under KPR-459.

Report `pendingExport` as recovery pending until it is actually cleared. A successful catalog commit remains readable and successful while its audit/change export is pending; a later replacement may be refused until projection succeeds. A failed recovery must not refresh discovery success or become a vendor error.

KPR-461 consumes the durable change collection and may add its own delivery wakeup/timer after routing is ready. It need not receive an in-memory scanner callback to avoid losing changes. This child never calls `Dispatcher`, selects a recipient, changes a delivery subdocument, or interprets catalog freshness as notification acknowledgment. KPR-461 should reuse this maintenance ownership where appropriate, rather than require a successful scan before considering pending/recovered notices.

## Shutdown contract

At the beginning of engine shutdown, call `stop` before closing Mongo or waiting through unrelated subsystem shutdown. It synchronously sets the stopped latch, clears the interval, aborts active discovery controllers, invalidates active lane invocations, and prevents subsequent scanner steps from starting new begin/discovery/apply/failure/recovery calls. Check that latch and invocation identity after awaited acquisition and discovery and before each new scanner-level store call. A late acknowledged `started`, even with remaining lease, cannot launch provider discovery from the stopped scanner.

Wait for the scanner's already-started tasks for at most `SHUTDOWN_DRAIN_MS = 5_000`, using an observed settlement race. Multiple `stop` calls share the same drain result. A late provider response is discarded. KPR-459's Claude cleanup and HTTP cancellation run; a started Grok credential rotation may finish its safe writeback as specified there, but cannot lead to a model fetch/apply after cancellation.

A store call already started before `stop` is not cancelable through the planned store API. It may finish its conditional acquisition or internal journaled commit/export sequence during or after the drain; preserve its outcome and observe rejection without starting another scanner operation. A late acquisition can leave active or expired interrupted running state when its original observation still matches; a newer scan fences it even after that newer attempt completes. Do not stamp a canceled/failed status over an uncertain apply or delayed begin, or claim that `stop` rolled back an accepted mutation. If the drain expires, leave the persisted attempt/envelope for restart recovery, log a fixed incomplete-drain diagnostic, and permit normal Mongo/process shutdown. No pending export is deleted. This bound concerns scanner shutdown only and does not promise the engine's other shutdown operations finish in five seconds.

## Truthful catalog and doctor diagnostics

Derive a provider status from the persisted document and an injected current time. Keep three independent facts visible:

1. **Saved catalog:** seeded/unseeded, model count, latest history-bearing write time, and provenance `manual`, `discovery`, or `legacy/unknown` when absent. Existing entries keep `source: "curated"` and `asOf = updatedAt`; that compatibility field is explicitly the saved-list timestamp.
2. **Successful discovery:** `scan.lastSucceededAt`, its nonnegative age when valid, and “recent” when age is less than eight hours, “overdue” at eight hours or older, or “never” when absent. An invalid/future date is “unknown/clock-inconsistent,” never recent. These labels describe the last discovery check, not proof that a later manual snapshot was validated.
3. **Latest attempt and storage:** attempt start/outcome, safe error and finish time when failed, running lease expiry or expired/unresolved state, next normal attempt time where known, plus recovery pending. A failed latest attempt remains visible even if the last successful check is still recent. A running/expired attempt does not fabricate an error, successful finish, or new freshness.

Use prose provider notes in `agent_model_catalog_list` for these facts, after the unchanged entries JSON. Include a status note for each requested built-in, including success, so unchanged checks have an observable fresh timestamp. For an unseeded built-in retain the existing unseeded meaning and manual-refresh option while explaining automatic discovery status. Plugin notes describe manual maintenance and any pending export without invented discovery expectations; Gemini remains on its existing live/cache/error path. A stored catalog remains listable when its latest discovery failed, with the failure and old successful-check time plainly attached. List calls do not trigger built-in discovery or export recovery.

Keep per-provider catalog reads contained so a provider-specific storage failure can produce an unavailable note alongside other successful results. Never convert an unreadable document to “not yet seeded.” A request for one stored provider whose state cannot be read returns a safe error; multi-provider reads return available entries plus unavailable notes. Preserve the existing Gemini-only hard-error behavior and all-provider partial behavior. Unknown-provider validation and legacy JSON entry ordering remain compatible.

The doctor's read-only adapter queries the catalog collection directly using the CLI's existing short-lived-client pattern and closes its client. It returns an explicit unavailable result for a failed database read, distinct from a successful read with missing catalogs. It must not instantiate discovery, recover exports, initialize catalog indexes, or import runtime configuration/SDK code through the status module. Render all three built-ins in the stable order and any stored plugin rows as manual; never query Gemini or imply a persisted Gemini baseline. Doctor freshness derives from catalog scan fields, not a newly written telemetry timestamp. The section is informational and does not change `allPassed` or the exit code.

Illustrative note: `codex: saved 2026-09-07T14:00:00Z (manual); last discovery success 2026-09-07T12:00:00Z (overdue); latest attempt failed at 20:00:12Z (auth); next attempt 2026-09-08T04:00:00Z.` A recent manual edit in this example is not shown as a successful vendor check. Recovery warnings are added independently when present; display strings are not a new machine-consumed API.

## Acceptance and verification contract

Use Vitest fake clocks/timers and injected discovery; no live provider credentials, generation, Slack messages, launchd restarts, or instance database writes. The lifecycle unit harness must execute the real scanner against a stateful store/fault harness or an exact behavioral store double, not only assert timer callbacks were invoked. Reuse KPR-459's planned catalog fake and isolated standalone Mongo harness for persistence integration. A missing required harness is setup work, not grounds to skip verification.

| Required flow | Observable assertions |
| --- | --- |
| First boot / legacy / empty catalog | One immediate attempt per eligible built-in; legacy manual `updatedAt` is not discovery history; active leases defer safely; status-only document remains unseeded; Gemini/plugins never discovered. |
| Cadence and restart | Seeded recent success/failure skips startup; exact eight-hour boundary is due; failures do not retry every minute; missed intervals do not burst; manual refresh changes neither due time nor successful freshness; unseeded startup override is consumed once per provider. |
| No overlap and isolation | Overlapping `tick`/`start` calls yield one provider lane; one delayed/rejecting provider, stuck store call, or recovery sweep leaves other lanes able to finish and start when due; no promise rejection leaks. |
| Concurrent scanners / atomic due observation | A and B read the same due state; A claims and completes; release B's delayed claim while B's proposal remains valid. B returns `busy`, makes no provider request, and preserves A's complete scan/catalog/history/change state. On the next tick it rereads and reevaluates cadence, without re-arming a consumed startup exception. Cover A changed success, unchanged success, and failure from seeded-overdue, missing, legacy-scanless, and expired-running states. Interleave a manual first seed to invalidate the seeded bit; seeded edits and export recovery alone must not reject an otherwise eligible claim. |
| Success / failure / unchanged | Compare persisted models, audit/change counts, catalog `updatedAt`, `scan.startedAt`, `lastSucceededAt`, and errors. Unchanged success changes only successful scan state. Provider/auth/empty/malformed/timeout failure preserves saved content and prior successful freshness. |
| Same-store fresh submission | Acquire through the scanner's long-lived store and prove a normal discovery commits. Acquiring through one store then accidentally constructing another for initial apply must fail the test's expected successful flow; no plan may paper over it by bypassing proof. |
| Ambiguous begin and completion | Delayed begin starts external discovery only after a current, uncanceled, unexpired `started` response; thrown begin retains its UUID until safe expiration and an outstanding store promise keeps its lane. Changed and unchanged ambiguous writes, unavailable evidence, guard engagement, and recovery failure never call failure recording or replay submission. Positive evidence updates the displayed truth without inventing a captured snapshot identity. |
| Expired proposal / accepted-but-expired response | Delay A before claim delegation until its proposal expires, with and without B acquiring/completing: the claim misses at its later operation timestamp and cannot overwrite B or grant proof. Separately freeze a fake claim's `$$NOW` before expiry and delay its mutation with unchanged observation, or hold a real matched acknowledgment until expiry: `started` records acceptance but creates no proof, starts no provider request, and changes no saved models/snapshot metadata/successful freshness/history/change data. B changing the observation before A's fake mutation instead forces a miss despite A's earlier timestamp. Recheck original dates, current invocation, cancellation, and local time after response and before request/apply; never renew a lease. |
| Restart / expired-token takeover | Fresh store performs reconciliation-only old-token calls. Active old lease prevents a new request. Expired running state, including an accepted-but-expired claim, permits bounded read/recovery then a fresh UUID only using the observation from the read that made takeover due. Delay an old begin as well as old success/failure until after the replacement starts and completes: each old mutation must miss and preserve the successor. Complete the predecessor between takeover's read and claim: takeover returns `busy`, then respects the observed completion's cadence. Include delayed missing-document shell/claim and never fabricate a failure during recovery. |
| Export maintenance | Startup and a later minute tick recover committed pending exports even when no provider is due, including a manual plugin change; duplicate recovery does not mutate delivery fields; a failed provider export does not cancel successful provider discovery. |
| Shutdown | Stop during acquisition, discovery, apply, failure recording, and recovery; no new scanner-level step begins afterward. Hold an acknowledged begin response until after stop/drain, both before and after its lease expiry: no provider request/apply/failure/recovery starts from it. Restart and acquire/complete a successor before releasing a delayed old begin/completion; assert real predicate misses and preservation of the newer state. Without a successor, a matched late claim may leave interrupted running state for restart recovery. Abort/discard late discovery, preserve uncertain writes, honor the five-second drain, clear timers, observe late rejections, and make repeated stop harmless. |
| Diagnostics | Tool and doctor agree for legacy/unseeded/recent/overdue/failed/running/expired/recovery-pending states, future/invalid dates, and read failure. Manual-after-success is distinguishable from checked content. JSON entry shape/order and Gemini/plugin regressions stay intact; raw injected secret-bearing errors never appear. |

Required real standalone-Mongo integration combines the scanner with KPR-459's store and injected discovery: startup → claim → committed/unchanged success; both scanners reading due before one completes and the other's delayed claim; expired-at-operation proposals with/without a successor; held matched acknowledgments past expiry; manual first seed between due read and claim and manual edit during discovery; failure preserving freshness; delayed ambiguous completion with negative reconciliation followed by late commit; restart/lease takeover with late old acquisition and completion; pending export recovery while discovery is not due; and shutdown with outstanding acquisition/completion. Run the changed/unchanged/failed predecessor and missing/legacy/expired-running variants above through the real scanner/store, asserting provider-call counts, actual matched counts, complete persisted scan/catalog state, and audit/change counts. Use real delegated operations/barriers; a barrier before delegation tests transit delay and a held matched acknowledgment tests post-response behavior, neither reproduces a server pause after `$$NOW` capture. Only the stateful fake's frozen-operation-timestamp barrier models that pause; it must accept the allowed expired-running outcome and test subsequent recovery, not assert wall-clock-at-mutation freshness. Reuse KPR-459's exact timestamp-equality, separate local/server clock, and malformed-lease predicate coverage rather than substituting a timer-only double. Reuse the owned temporary Mongo process, never an application URI or existing server. Application-to-provider and CoS/Slack E2E behavior belongs to KPR-461; this child's engine wiring is checked locally without a live boot.

Implementation must pass targeted scanner/status/admin/doctor tests, the real scanner/store integration suite, relevant boot-order regressions, `npm run check`, and `npm run build`. The plan must provide concrete test files/commands and account for the shared KPR-459 harness being planned rather than already implemented. No product test execution or verification success is claimed by this specification.

## Assumptions and review notes

- Non-blocking delegated choice: cadence is anchored to the persisted attempt start, not success. A recent failed attempt therefore leaves visibly overdue successful freshness while waiting for its next eight-hour attempt. Unseeded startup and expired interrupted work are the explicit immediate-recovery exceptions. An unseeded startup opportunity is consumed at its due decision, so a refused claim cannot reuse it after a concurrent failure.
- Non-blocking delegated choice: the scanner is always enabled for the three built-ins, with fixed production intervals. Discovery controls its own existing credential availability; adding disable/provider/cadence configuration is unnecessary for this ticket.
- Non-blocking delegated choice: one minute is a maintenance/recovery poll, two minutes is a nonrenewed attempt lease, and five seconds bounds scanner drain. A persistently stuck store call can stall that provider until restart; preserving no-overlap and uncertainty is preferable to spawning unbounded operations.
- Non-blocking compatibility constraint: entry `asOf` retains its saved-catalog meaning; successful-check freshness appears in explicit status notes. A successful prior check is never an assertion that an intervening manual replacement was checked against the vendor.
- Non-blocking dependency limitation: KPR-459's proof and unchanged reconciliation are conservative. A freshly constructed store can observe successful scan state without being able to reconstruct an old unchanged operation's snapshot identity; this child uses that state for scheduling/freshness and never manufactures a replacement result.
- Non-blocking dependency limitation: operation-constant `$$NOW` can admit a claim that becomes expired before mutation/acknowledgment. Same-read observation fencing protects successors; post-response invocation/cancellation/lease checks prevent new provider work from an expired acknowledgment. Accepted expired state remains interrupted work for bounded recovery and fresh-observation takeover, with no fabricated freshness or failure.
- No blocking product question remains. These choices fit the approved Gate 1 scope and must be independently reviewed before planning.
